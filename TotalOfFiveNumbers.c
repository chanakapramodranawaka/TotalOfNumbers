#include <stdio.h>
int main(void)
{
    int count = 0,total;

    if(count>=5)
    {
        total = total+count;
        count = count +1;
    }

    else
    {
        printf("Total is %d",total);
    }

    return 0;
}
